# TEMPLATE — never commit real values.
# Populate with:
#   kubectl create secret generic misterfritz-secrets \
#     --from-literal=DISCORD_BOT_TOKEN=<token> \
#     --from-literal=ROOT_USER=<username> \
#     -n misterfritz
#
# For production use External Secrets Operator or Sealed Secrets instead.
apiVersion: v1
kind: Secret
metadata:
  name: misterfritz-secrets
  namespace: misterfritz
type: Opaque
stringData:
  DISCORD_BOT_TOKEN: "REPLACE_ME"
  ROOT_USER: "REPLACE_ME"
